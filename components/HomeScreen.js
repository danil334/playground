import React from "react";
import { Text, SafeAreaView, TouchableOpacity } from "react-native";

const HomeScreen = () => {
    return (
        <SafeAreaView>
            <Text>Move to counter:</Text>
            <TouchableOpacity></TouchableOpacity>
        </SafeAreaView>
    );
};

export default HomeScreen;
